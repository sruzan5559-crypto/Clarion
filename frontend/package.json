{
  "name": "clarivon-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts --host",
    "build": "vite build --config vite.config.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
